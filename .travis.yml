language: node_js
node_js:
  - "stable"

sudo: true

addons:
  chrome: stable

branches:
  only:
    - master

cache:
  directories:
    - node_modules

before_install:
  - npm install -g @angular/cli

install:
  - npm install

script:
  - npm run test:ci
